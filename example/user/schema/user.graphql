"test user"
type User implements HasName @key(fields: "id") @model {
  name: String! @index
  posts: [Post!]! @paginate
}

extend type Query {
  users: [User!]! @paginate
}

interface Userable {
  userId: ID!
  user: User!
}