"test user"
type User implements HasName @key(fields: "id") @model {
  name: String! @index
  posts: [Post!]!
}

extend type Query {
  users: [User!]! @paginate
  user(id: ID!): User! @first
  post(id: ID!): Post! @first
}

interface Userable {
  userId: ID!
  user: User!
}