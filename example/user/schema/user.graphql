"test user"
type User @key(fields: "id") @model {
  name: String! @index
  posts: [Post!]! @paginate
}

extend type Query {
  users: [User!]! @paginate
}

interface Userable {
  userId: ID!
  user: User!
}